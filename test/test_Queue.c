#include "unity.h"
#include "Queue.h"

//Enqueue takes a pointer and keeps it
//Dequeue returns the oldest pointer and removes it
//Peek returns the oldest pointer but keeps it
//Count returns the number of elements
//Contains determines if the queue already has this element

void setUp(void)
{
}

void tearDown(void)
{
}

void test_Queue_NeedToImplement(void)
{
    TEST_IGNORE_MESSAGE("Need to Implement Queue");
}
